<div class="m-5 flex flex-col items-center">
  <div class='m-3 text-3xl'>プロフィール</div>

  <table class="m-3 table">
    <tr>
      <th >name</th>
      <td><%= current_user.name %></td>
    </tr>
    <tr>
      <th>email</th>
      <td><%= current_user.email %></td>
    </tr>
  </table>

  <%= link_to '編集', edit_profile_path, class: 'mx-5 w-20 btn btn-secondary rounded-full' %>

</div>